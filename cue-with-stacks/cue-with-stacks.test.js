const CueWithStacks = require('./cue-with-stacks');
describe('CueWithStacks methods', () => {
  
    let queue = [10, 15, 20];
  it('has an encue method', () => {
    const cue = new PseudoQueue();

    
  });